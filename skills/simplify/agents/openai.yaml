interface:
  display_name: "Simplify"
  short_description: "Simplify recent code without behavior changes"
  default_prompt: "Use $simplify to refine the current diff while preserving observable behavior and unrelated workspace changes."

